[pytest]
markers =
    unit: unit tests (fast, no external dependencies)
    integration: integration tests that may require external services (e.g., Ollama)
    api: tests for Flask API endpoints
    safety: safety and security related tests
    ollama: marks tests that need Ollama server availability
